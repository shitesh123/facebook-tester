language: node_js

node_js:
  - 12

addons:
  apt:
    packages:
      # Ubuntu 16+ does not install this dependency by default, so we need to install it ourselves
      - libgconf-2-4

install:
  - npm ci

before_script:
  - npm start -- --silent &

script:
  - npm run cypress:run
